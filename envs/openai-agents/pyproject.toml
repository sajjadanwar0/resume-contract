[project]
name = "rc-env-openai-agents"
version = "0.1.0"
description = "Resume Contract matrix cell: OpenAI Agents SDK (latest at lock time). Live-keyed probes only (OPENAI_API_KEY)."
requires-python = ">=3.12"
dependencies = [
  "openai-agents==0.18.2",
]

[tool.uv]
package = false
